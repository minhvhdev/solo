import Head from 'next/head';
import Link from 'next/link';

import { PlayingCard } from '@common/components/atoms';
import { PLAYING_CARDS_FULL } from '@common/constants';
import { DATA_GAMES } from '@common/data/games.data';

const Home = () => {
  return (
    <>
      <Head>
        <title>Solo</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>
        {DATA_GAMES.map((game) => (
          <Link key={game.name} href={`/games/${game.url}`}>
            <div>
              <span>{game.name}</span>
            </div>
          </Link>
        ))}
        <div className="flex h-[300px]">
          {PLAYING_CARDS_FULL.map((card) => (
            <PlayingCard key={card.name} name={card.name} />
          ))}
        </div>
      </div>
    </>
  );
};

export default Home;
